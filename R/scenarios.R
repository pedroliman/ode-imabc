
# This file defines the inputs included in the scenarios to be run:
# This list is defined here so it can be used outside of the run_model.R file.

scenarios_list = list(
  "1" = list(range_check = FALSE),
  "2" = list(range_check = TRUE)
)